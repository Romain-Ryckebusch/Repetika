apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.global.postgres.host }}   # "postgres"
  namespace: {{ .Values.global.namespace }}
spec:
  selector:
    app: postgres
  ports:
    - name: postgres
      port: {{ .Values.global.postgres.port }}   # 5432
      targetPort: 5432
      protocol: TCP
