apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.global.mongo.host }}   # "mongodb"
  namespace: {{ .Values.global.namespace }}
spec:
  selector:
    app: mongodb
  ports:
    - name: mongo
      port: {{ .Values.global.mongo.port }}   # 27017
      targetPort: 27017
      protocol: TCP
