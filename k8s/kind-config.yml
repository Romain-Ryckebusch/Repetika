# k8s/kind-config.yml
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: 30080    # K8s Service/Ingress for API
        hostPort: 8080
        protocol: TCP
      - containerPort: 3000  # Grafana (optional)
        hostPort: 3000
        protocol: TCP
      - containerPort: 9090  # Prometheus (optional)
        hostPort: 9090
        protocol: TCP

